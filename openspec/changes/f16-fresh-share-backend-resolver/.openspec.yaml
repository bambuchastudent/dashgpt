schema: spec-driven
created: 2026-08-11
goal: Make freshly-created public ChatGPT Share links resolve immediately by consuming the current public share JSON through a resolver instead of relying on legacy HTML payload parsing.
