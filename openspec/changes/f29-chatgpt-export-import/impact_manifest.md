# F29 Impact Manifest

Production overlap: current ChatGPT history import UI/controller, canonical Vault card writes, and a new local export parser. Test overlap: import/dedup/mobile regressions. No new card type and no Vault schema migration. Existing live import remains available.
