# F29 impact

Touches the existing ChatGPT history import operation, a new local export parser, existing canonical Vault card writes, import UI styling, and regression tests. No new card type or Vault schema migration. The selected export remains local browser input and is not persisted as a separate asset. Live import remains available.
