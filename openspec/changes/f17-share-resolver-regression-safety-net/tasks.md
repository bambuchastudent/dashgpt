# Tasks

- [x] Define deterministic current-backend parser regression matrix.
- [x] Add parser-focused verification to the normal `npm run check` chain.
- [x] Extend browser onboarding coverage for manual Share fallback and direct `?share=` handoff.
- [x] Add recurring/live production Share smoke workflow using multiple real fixtures.
- [x] Ensure live smoke retries bounded transient failures and remains provider-neutral.
- [x] Validate OpenSpec change.
- [x] Run full repository and browser verification.
- [x] Open separate PR and merge only when required CI is green.
- [x] Confirm both real public fixtures satisfy the production resolver contract before merge; the merged workflow will continue checking them after every `develop` push and every 6 hours.
