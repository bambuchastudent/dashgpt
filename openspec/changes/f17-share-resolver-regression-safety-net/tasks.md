# Tasks

- [x] Define deterministic current-backend parser regression matrix.
- [ ] Add parser-focused verification to the normal `npm run check` chain.
- [ ] Extend browser onboarding coverage for manual Share fallback and direct `?share=` handoff.
- [ ] Add recurring/live production Share smoke workflow using multiple real fixtures.
- [ ] Ensure live smoke retries bounded transient failures and remains provider-neutral.
- [ ] Validate OpenSpec change.
- [ ] Run full repository and browser verification.
- [ ] Open separate PR and merge only when required CI is green.
- [ ] Run live production smoke after merge and require both real fixtures to pass.
