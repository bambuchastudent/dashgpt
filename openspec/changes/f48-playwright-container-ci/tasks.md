# Tasks

- [x] Inspect current hosted workflow, Playwright dependency pin and F47 overlap.
- [x] Confirm run `34385670227` failed before browser tests because `playwright install --with-deps chromium` hit an external APT `Hash Sum mismatch`.
- [x] Confirm Microsoft publishes `mcr.microsoft.com/playwright:v1.62.1-noble` and that the version matches DashGPT `@playwright/test` `1.62.1`.
- [x] Prepare F48 proposal, design, CI-verification spec delta and Impact Manifest.
- [x] Strictly validate the original F48 OpenSpec before production workflow edits (`34400228372`), then revalidate the initial implementation head (`34400375802`).
- [x] Run browser matrix inside the pinned official Playwright container with host IPC; desktop and mobile both passed in `34400375830`.
- [x] Remove runtime `npx playwright install --with-deps chromium` from browser-bearing jobs.
- [x] Update deterministic hosted-CI regression verification for image/version coupling and absence of runtime browser installation.
- [x] Expand F48 OpenSpec before cache implementation to include reusable `node_modules` caching and install-on-miss semantics.
- [ ] Strictly validate the cache-expanded F48 OpenSpec before changing dependency setup in the workflow.
- [ ] Add versioned `node_modules` caching to deterministic, browser and canonical-full jobs.
- [ ] Skip `npm install --ignore-scripts --no-audit --no-fund` only on exact cache hits; preserve it on cache misses.
- [ ] Extend deterministic hosted-CI regression verification for cache key/path and conditional install behavior.
- [ ] Run targeted deterministic verification on the cache implementation head.
- [ ] Run hosted desktop and mobile browser shards on the final implementation head.
- [ ] Run a repeated unchanged-dependency hosted run and confirm exact cache hit(s) skip npm installation.
- [ ] Run one opt-in literal `npm run verify:full` hosted lane on the final implementation head.
- [ ] Confirm final required `check` passes on the final implementation head.
- [ ] Merge the F48 PR into `develop` only after the required evidence is green.
